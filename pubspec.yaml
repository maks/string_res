name: string_res
description: A generator of strings constants from json
version: 1.2.0
homepage: https://github.com/maks/string_res

environment:
  sdk: '>=2.5.0 <3.0.0'

dependencies:
  meta: ^1.1.6
  build: ^1.1.4
  build_config: ^0.4.0
  dart_style: ^1.2.8
  glob: ^1.2.0
  path: ^1.6.4

dev_dependencies:
  pedantic: ^1.9.0
  test: 
